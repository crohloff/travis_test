language: c

services:
  - docker
  
env: SH="docker exec -t travis-test-image bash -c"

before_install:
  - docker build -t travis-test .
  - docker run -d --name travis-test-image travis-test
  - docker ps

script:
  - $SH "/test.sh build"
