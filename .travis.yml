language: c

services:
  - docker
  
env: SH="docker exec -t travis-test bash -c"

before_install:
  - docker build -t travis-test .
  - docker run -d travis-test
  - docker ps

script:
  - $SH "/test.sh build"
